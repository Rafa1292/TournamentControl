@page "/Players/Delete/{PlayerId}"


<DeletePlayerComponent PlayerId="PlayerId"/>

@code {
    [Parameter]
    public string PlayerId { get; set; }

    protected override async Task OnInitializedAsync()
    {
        PlayerId = PlayerId;
    }
}