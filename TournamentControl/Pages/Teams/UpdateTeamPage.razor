@page "/Teams/Update/{TeamId}"


<UpdateTeamComponent TeamId="@TeamId" />

@code {
    [Parameter]
    public string TeamId { get; set; }

    protected override async Task OnInitializedAsync()
    {
        TeamId = TeamId;
    }
}